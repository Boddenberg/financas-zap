{
  "name": "financas-zap",
  "version": "1.0.0",
  "private": true,
  "description": "Ponte local que avisa no WhatsApp quando uma limpeza é registrada no app Casa.",
  "packageManager": "npm@11.6.2",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "test:message": "tsx src/index.ts --test-message",
    "list:groups": "tsx src/index.ts --list-groups",
    "test": "tsx --test src/*.test.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "contracts:generate": "openapi-typescript contracts/openapi.json -o src/api-contract.d.ts",
    "contracts:check": "npm run contracts:generate && git diff --exit-code -- src/api-contract.d.ts"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.7"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/qrcode-terminal": "^0.12.2",
    "openapi-typescript": "^7.13.0",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "brace-expansion": "^5.0.8"
  }
}
